import { FilterPattern } from '@rollup/pluginutils';
import { Plugin, InputPluginOption } from 'rollup';
import { IExtensionManifest } from './vscode/vs/platform/extensions/common/extensions.js';
import { ExtensionResource } from './dist/types/src/extension-tools.js';

interface Options {
    include?: FilterPattern;
    exclude?: FilterPattern;
    rollupPlugins?: InputPluginOption[];
    transformManifest?: (manifest: IExtensionManifest) => IExtensionManifest;
    getAdditionalResources?: (manifest: IExtensionManifest) => Promise<ExtensionResource[]>;
}
declare function plugin({ include, exclude, transformManifest, getAdditionalResources }?: Options): Plugin;

export { plugin as default };
