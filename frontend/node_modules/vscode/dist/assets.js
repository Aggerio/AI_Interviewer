let assetUrls = {};
function registerAssets(assets) {
    assetUrls = {
        ...assetUrls,
        ...assets
    };
}
function toUrl(name) {
    let url = assetUrls[name];
    if (typeof url === 'function') {
        url = url();
    }
    return ( new URL(url, window.location.href).toString());
}
window.monacoRequire = {
    toUrl
};
export { registerAssets };
